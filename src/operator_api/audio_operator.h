#pragma once
#include "operator_api/types.h"

// VividAudioContext is now defined in types.h.
// This header is retained for backward-compatible includes —
// operators that #include "operator_api/audio_operator.h" continue to work.
