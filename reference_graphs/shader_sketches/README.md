# Shader Sketches

Small WGSL sketch examples that are useful as shader references and visual experiments.

File-backed texture-loader studies live in `../media/texture_loader/`.

They are useful for shader/operator reference and visual regression checks, but they are not part of
the default beta onboarding path.
