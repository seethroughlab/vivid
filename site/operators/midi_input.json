{
  "name": "MidiInput",
  "id": "midi_input",
  "domain": "control",
  "source_file": "operators/control/midi_input/midi_input.cpp",
  "brief": "Hardware MIDI input translated into native notes_out plus auxiliary control lanes.",
  "description": "Connects to a MIDI input device and emits notes_out as the canonical\ninternal note stream for synth notes_in ports. It also provides note/velocity/gate\nas scalar convenience signals and auxiliary lane arrays (up to 16 held notes),\nplus pitch bend, mod wheel, a learnable CC value, and per-note expression lanes.\npoly_shared broadcasts shared expression to all notes, while mpe_lower and\nmpe_upper route per-channel expression to individual note lanes for MPE controllers.",
  "tips": [
    "Use notes_out -> notes_in for the default synth path; add NoteBreakout only when you need shared per-voice control lanes.",
    "Use mpe_lower or mpe_upper mode with an MPE controller for per-note expression."
  ],
  "related": [
    "DrumKit",
    "Arpeggiator",
    "Keyboard"
  ],
  "has_docs": true,
  "params": [
    {
      "name": "channel",
      "doc": "MIDI channel filter. 0 = omni (all channels)."
    },
    {
      "name": "mode",
      "doc": "Expression routing mode: poly_shared (broadcast), mpe_lower (manager ch1, members ch2-15), mpe_upper (manager ch16, members ch15-2)."
    }
  ],
  "inputs": [],
  "outputs": [
    { "name": "note", "type": "scalar", "doc": "Latest MIDI note number (0-127)." },
    { "name": "velocity", "type": "scalar", "doc": "Latest velocity (0-1)." },
    { "name": "gate", "type": "scalar", "doc": "1 if any notes held, 0 otherwise." },
    { "name": "trigger", "type": "scalar", "doc": "1 on note-on frame, 0 otherwise." },
    { "name": "pitch_bend", "type": "scalar", "doc": "Global pitch bend (-1 to 1)." },
    { "name": "mod_wheel", "type": "scalar", "doc": "CC1 mod wheel (0-1)." },
    { "name": "cc_value", "type": "scalar", "doc": "Learnable CC value (0-1)." },
    { "name": "notes", "type": "lane_array", "doc": "Held note numbers as lane array." },
    { "name": "velocities", "type": "lane_array", "doc": "Per-note velocities (0-1)." },
    { "name": "gates", "type": "lane_array", "doc": "Per-note gates (always 1 for held notes)." },
    { "name": "notes_out", "type": "custom", "doc": "Native note stream for downstream synth notes_in ports." },
    { "name": "aftertouch", "type": "scalar", "doc": "Channel aftertouch (0-1)." },
    { "name": "expression", "type": "scalar", "doc": "CC11 expression (0-1)." },
    { "name": "lane_ids", "type": "lane_array", "doc": "Stable identity token per active note." },
    { "name": "pitch_bends", "type": "lane_array", "doc": "Per-note pitch bend (-1 to 1)." },
    { "name": "pressures", "type": "lane_array", "doc": "Per-note pressure (0-1)." },
    { "name": "slides", "type": "lane_array", "doc": "Per-note slide/CC74 (0-1)." },
    { "name": "expressions", "type": "lane_array", "doc": "Per-note expression/CC11 (0-1)." },
    { "name": "channels", "type": "lane_array", "doc": "Per-note MIDI channel (1-16)." }
  ]
}
