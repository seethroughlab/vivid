# Examples will be added here as they are implemented
# Each example will be a separate executable that uses the Vivid runtime

# Example: Basic test (Phase 1 completion)
# add_subdirectory(basic-test)
