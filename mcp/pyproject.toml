[project]
name = "vivid-mcp"
version = "0.1.0"
description = "MCP bridge for the Vivid (proxies tool calls to the app's loopback control server)"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
